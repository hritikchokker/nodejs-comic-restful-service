const UserModel = require('../models/usermodel');
const userOperations = {
    login(response){
        const user = [{
            "username": "Hritik",
            "password": "mysecret"
        }]
    }
}
module.exports = userOperations;